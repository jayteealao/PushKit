package com.pushkit.app.data.upload

/** Single source of truth for upload fallback defaults shared across the upload package. */
internal object UploadDefaults {
    const val CONTENT_TYPE = "application/octet-stream"
    const val FILENAME = "upload.bin"
}
